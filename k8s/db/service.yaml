apiVersion: v1
kind: Service
metadata:
  name: crm-db-service
  namespace: crm-production
  labels:
    app: crm-db
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
  selector:
    app: crm-db
    tier: database
